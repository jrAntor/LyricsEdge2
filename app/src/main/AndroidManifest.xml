<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a.lyricaledge2">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Wikia" />
        <activity android:name=".Metro" />
        <activity android:name=".Genius" />
        <activity android:name=".AZ" />
        <activity android:name=".GreenP" />
        <activity android:name=".FreakLy" />
        <activity android:name=".LyricsDot" />
        <activity android:name=".LyTrain"></activity>
    </application>

</manifest>